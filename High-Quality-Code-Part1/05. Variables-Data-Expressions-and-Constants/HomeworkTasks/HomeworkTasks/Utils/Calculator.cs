using System;

namespace HomeworkTasks.Utils
{
    public static class Calculator
    {
        public static double AbsoluteCos(double angle)
        {
            return Math.Abs(Math.Cos(angle));
        }

        public static double AbsoluteSin(double angle)
        {
            return Math.Abs(Math.Sin(angle));
        }
    }
}
